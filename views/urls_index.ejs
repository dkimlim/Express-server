<!DOCTYPE html>
<html lang="en">
<head>
  <!-- <title>TinyApp</title> -->
  <% include ./partials/_header %>
</head>

<body>

	<h1> Your Short URLs</h1>

<% for (var key in urls) { %>
	
    <ul>
    	<li> <a href ="localhost:8080/<%= key %>">http://localhost:8080/<%= key %></a> <%= " (" + urls[key] + ") "%> </li>
    	
    </ul>
<% } %>


</body>

</html>