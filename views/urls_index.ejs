<!DOCTYPE html>
<html lang="en">

	<head>
	  <title>TinyApp</title>
	</head>

	<body style="font-size:20px; padding:50px;">

	<header><% include ./partials/_header %></header>

		<h1> Your Short URLs</h1>

	<% for (var key in urls) { %>
		
	    <ul>
	    	<li> <a href ="localhost:8080/<%= key %>">http://localhost:8080/<%= key %></a> <%= " (" + urls[key] + ") "%> 

	    		<form method="POST" action="/urls/<%= key %>/delete"><input type="submit" value="Don't press!"></form></li>	    	
	    </ul>

	<% } %>

	</body>

</html>